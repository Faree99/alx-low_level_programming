#include "main.h"

/**
 * print_number - prints integers
 * Return: Always 0
 */
void print_number(int n)
{
	int n;

	n = 0;
	while
		(n < 10)
		{
			_putchar(n + '0');
			n++;
		}
	putchar('\n');
	return (0);
}
